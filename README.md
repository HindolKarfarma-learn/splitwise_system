# splitwise_system

### add your own .env file and write
```python
DB_URL_SQL=mysql+aiomysql://root:Password@localhost:3306/Database
```

put your database and its password 
change the root to the user name if you have a designated user